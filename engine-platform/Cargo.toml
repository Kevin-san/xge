[package]
name = "engine-platform"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Game Engine Team"]
license = "MIT OR Apache-2.0"
description = "Game engine platform abstraction library"

[dependencies]
# 无外部依赖，使用 std::sync 替代 parking_lot

[dev-dependencies]