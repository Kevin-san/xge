[package]
name = "engine-utils"
version = "0.1.0"
edition = "2021"
description = "Game engine utility types"

[dependencies]
parking_lot = { workspace = true }
uuid = { workspace = true }
serde = { workspace = true }

[features]
default = ["std"]
std = []
